function openNav() {
    const modal = document.querySelector('.dashboard-nav-container');
    modal.classList.add('dashboard-nav-container--open');
}

function closeNav() {
    const modal = document.querySelector('.dashboard-nav-container');
    modal.classList.remove('dashboard-nav-container--open');
}
